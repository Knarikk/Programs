int Search(const vector<int> &v, int x)   
{ 
    for(unsigned i=0;i<v.size();i++)
    { 
        if(v[i]==x)
        {
            return i;
        }
    }
    return -1;
}
void print(const vector<int>& v)
{
    for (unsigned i = 0; i < v.size(); ++i) 
    {
        cout << v[i] << " ";
    }
}
